class Solution {
    public int myAtoi(String s) {
        int MAX = Integer.MAX_VALUE;
        int MIN = Integer.MIN_VALUE;
        if( s != null && s.length() > 0){
            int index = 0;
            int len = s.length();
            while(index < len && s.charAt(index) == ' ')
                index++;
            if(index == len)
                return 0;
            char firstChar = s.charAt(index);
            if(Character.isDigit(firstChar) || firstChar == '+' || firstChar == '-'){
                boolean isNegative = false;
                if(!Character.isDigit(firstChar)){
                    isNegative = firstChar == '-';
                    index++;
                }                    
                int ans = 0;
                while(index < len && Character.isDigit(s.charAt(index))){
                    int digit = s.charAt(index)-'0';
                    if(ans > MAX/10 || (ans == MAX/10 && digit > 7))
                        return isNegative ? MIN : MAX;
                    ans = ans*10 + digit;
                    index++;                        
                } 
                return isNegative ? -1*ans : ans;                
            }else{
                return 0;
            }            
        }
        return 0;
    }
}

Dear  Sir/Madam, Hope you aredoing well. I am reachingout to you requesting the copy of approval notice for the case EAC-19-178-52136.  My I-129 (EAC1917852101) wasfiled in premium processing on May 15, 2019 and was approved on Jun 102019. Along with mycase, cases were also filed for my dependents (i.e. my spouse and my daughter) My Spouse'scase number is EAC1917852135. and was approved in July 2019 and the approval noticewas received.  MyDaughter's case number is EAC1917852136 and the case status inUSCIS website still shows as Case Received. When I contacted USCIS aboutthe case status they informed that the case was approved and mailed in July2019. However I didn't receive the approval notice yet.  I request you to kindly share a copy of the approval for the same. I am attaching the below documents for your reference.1. Approval notice of I-129 (EAC1917852101)2.Approval notice of I-539 for my spouse (EAC1917852135)3. Receipt notice of my daughter's case (EAC1917852136)4. Biometric appointment notice of my daughter (EAC1917852136)  Please feel free to call me on my mobile 210-452-9696 if you need any additional information.
Looking forward to hearing from you. Thankyou very much.  RegardsRathnakar
